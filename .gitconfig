[user]
	name = Sinisa Kusic
	email = contact.sinisa@gmail.com
	signingkey = ~/.ssh/id_rsa.pub

[init]
	defaultBranch = main

[core]
	pager = delta
	editor = nvim
	whitespace = trailing-space,space-before-tab
	excludesfile = ~/.gitignore_global

[push]
	default = simple
	autoSetupRemote = true

[pull]
	rebase = true
	ff = only

[rebase]
	autoStash = true
	autoSquash = true

[merge]
	tool = diffview
	conflictstyle = zdiff3

[mergetool]
	prompt = false
	keepBackup = false

[mergetool "diffview"]
	cmd = nvim -f -c "DiffviewOpen"

[diff]
	tool = nvimdiff
	colorMoved = default

[rerere]
	enabled = true
	autoUpdate = true

[branch]
	sort = -committerdate

[tag]
	sort = -version:refname

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true
	line-numbers = true
	side-by-side = false
	syntax-theme = Monokai Extended
	file-style = bold yellow ul
	hunk-header-style = file line-number syntax
	hunk-header-decoration-style = blue ul

[color]
	ui = auto
	interactive = true
	status = true
	branch = true
	diff = true

[commit]
	template = ~/.gitmessage
	gpgsign = true

[gpg]
	format = ssh

[gpg "ssh"]
	program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[includeIf "gitdir:~/Projects/Spacelift/"]
	path = ~/Projects/Spacelift/.gitconfig
